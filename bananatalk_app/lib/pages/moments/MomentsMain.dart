import 'package:flutter/material.dart';

class MomentsMain extends StatelessWidget {
  const MomentsMain({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Moments Main'),
      ),
    );
  }
}
